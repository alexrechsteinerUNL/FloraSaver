<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:ios="clr-namespace:Microsoft.Maui.Controls.PlatformConfiguration.iOSSpecific;assembly=Microsoft.Maui.Controls"
             x:Class="FloraSaver.TablePage"
             xmlns:model="clr-namespace:FloraSaver.Models"
             xmlns:viewmodel="clr-namespace:FloraSaver.ViewModels"
             x:DataType="viewmodel:TableViewModel"
             Title="Plant Table">
    <!--<Shell.SearchHandler>
        <SearchHandlers:PlantSearchHandler
            Placeholder="Enter search term"
            ShowsResults="true"
            DisplayMemberName="Name"
            ItemsSource="{Binding Plants}">
            <SearchHandlers:PlantSearchHandler.ItemTemplate>
                <DataTemplate>
                    <HorizontalStackLayout Padding="10">
                        <Label Text="{Binding GivenName}"
                           Margin="0,0,10,0" />
                        <Label Text="Test" />
                    </HorizontalStackLayout>
                </DataTemplate>
            </SearchHandlers:PlantSearchHandler.ItemTemplate>
        </SearchHandlers:PlantSearchHandler>
    </Shell.SearchHandler>-->

    <Grid
        ColumnDefinitions="*,*"
        ColumnSpacing="5"
        RowSpacing="1">
        <Grid.RowDefinitions>
            <RowDefinition Height="50" />
            <RowDefinition Height="*" />
            <RowDefinition Height="75" />
        </Grid.RowDefinitions>
        <SearchBar Grid.Row="0" Grid.ColumnSpan="2" Placeholder="Search items..." />
        <RefreshView
            Grid.Row="1"
            Grid.ColumnSpan="2"
            Command="{Binding GetPlantsCommand}"
            IsRefreshing="{Binding IsRefreshing}">
            <CollectionView 
            ItemsSource="{Binding Plants}"
            SelectionMode="None"
                >
                <CollectionView.ItemTemplate>
                    <DataTemplate x:DataType="model:Plant">
                        <Grid Padding="10" >
                            
                            <Frame HeightRequest="250" BackgroundColor="Black">
                                <Frame.GestureRecognizers>
                                    <TapGestureRecognizer
                                    Command="{Binding Source={RelativeSource AncestorType={x:Type viewmodel:TableViewModel}}, Path=GoToDetailsCommand}"
                                    CommandParameter="{Binding .}"/>
                                </Frame.GestureRecognizers>
                                
                                <Grid Padding="0" ColumnDefinitions="125,*" >
                                    <Image Aspect="AspectFill" Source="{Binding ImageLocation}"
                                       WidthRequest="125"
                                       HeightRequest="125"/>
                                    <VerticalStackLayout
                                    Grid.Column="1"
                                    Padding="10">
                                        <Label FontSize="36" Text="{Binding GivenName, StringFormat='{0}'}" />
                                        <Label FontAttributes="Italic" Text="{Binding PlantSpecies}" />
                                        <Label Text="{Binding DateOfLastWatering, StringFormat='Last Watered: {0}'}" />
                                        <Label Text="{Binding DateOfBirth, StringFormat='Birthday: {0}'}" />
                                        <Label FontSize="24" Text="{Binding DateOfNextWatering, StringFormat='Next Watering: {0}'}" />
                                    </VerticalStackLayout>
                                </Grid>
                            </Frame>
                        </Grid>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </RefreshView>
        <Button Text="Get Plants" 
                x:Name="getPlants"
                Command="{Binding GetPlantsCommand}"
                IsEnabled="{Binding IsNotBusy}"
                Grid.Row="2"
                Grid.Column="0"
                Style="{StaticResource ButtonOutline}"
                Margin="8"/>
        <Button Text="Add New Plant" 
                x:Name="addPlants"
                Command="{Binding Source={RelativeSource AncestorType={x:Type viewmodel:TableViewModel}}, Path=GoToDetailsCommand}"
                IsEnabled="{Binding IsNotBusy}"
                Grid.Row="2"
                Grid.Column="1"
                Style="{StaticResource ButtonOutline}"
                Margin="8"/>

        <ActivityIndicator IsVisible="{Binding IsBusy}"
                           IsRunning="{Binding IsBusy}"
                           HorizontalOptions="FillAndExpand"
                           VerticalOptions="CenterAndExpand"
                           Grid.RowSpan="2"
                           Grid.ColumnSpan="2"/>
    </Grid>
</ContentPage>